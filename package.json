{
  "name": "bikeparking-client",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "bundlestage": "cd bicycleparking &&  npm install && npm install --dev && npm run stage",
    "bundleprod": "cd bicycleparking &&  npm install && npm install --dev && npm run prod",
    "postinstall": "cd bicycleparking && npm install && npm install --dev && npm run stage"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "flatpickr": "^4.0.6",
    "leaflet-search": "^2.3.6",
    "npm": "^6.14.6"
  }
}
